# Django Settings
SECRET_KEY=django-insecure-your-secret-key-here
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database Configuration - MySQL
DATABASE_ENGINE=django.db.backends.mysql
DATABASE_NAME=RifaTrust
DATABASE_USER=root
DATABASE_PASSWORD=tu_contraseña
DATABASE_HOST=localhost
DATABASE_PORT=3306

# Email Configuration
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@example.com
EMAIL_HOST_PASSWORD=your-password

# Payment Gateway (Stripe)
STRIPE_PUBLIC_KEY=pk_test_your_public_key
STRIPE_SECRET_KEY=sk_test_your_secret_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# Email Verification API (AbstractAPI)
# Obtén tu API key gratuita en: https://www.abstractapi.com/api/email-verification-validation-api
# Plan gratuito: 100 verificaciones/mes sin tarjeta de crédito
EMAIL_VERIFICATION_API_KEY=your_abstractapi_key_here

# Email Configuration (para envío de emails de confirmación)
# Opción 1: Console (desarrollo - muestra emails en consola)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend

# Opción 2: Gmail (producción - requiere App Password)
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=tu_email@gmail.com
# EMAIL_HOST_PASSWORD=tu_app_password_aqui
# DEFAULT_FROM_EMAIL=RifaTrust <noreply@rifatrust.com>

# Site Configuration
SITE_DOMAIN=localhost:8000
SITE_URL=http://localhost:8000

# CORS Settings
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Media and Static Files
MEDIA_URL=/media/
STATIC_URL=/static/

# Security Settings (para producción)
# SECURE_SSL_REDIRECT=True
# SESSION_COOKIE_SECURE=True
# CSRF_COOKIE_SECURE=True
# SECURE_HSTS_SECONDS=31536000
# SECURE_HSTS_INCLUDE_SUBDOMAINS=True
# SECURE_HSTS_PRELOAD=True
